import React from 'react';
import SVG from 'react-inlinesvg';
import { toAbsoluteUrl } from '_metronic/_helpers';

const CMSList = ({ className }) => {
  return <></>;
};

export default React.memo(CMSList);
